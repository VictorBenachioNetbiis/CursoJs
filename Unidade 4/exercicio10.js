function soma(a,b) {
    return a + b;
}
console.log(soma(1,2));

let subtracao = function(a,b) {
    return a - b;
}

console.log(subtracao(1,2));


let divisao = (a,b) => a / b;
console.log(divisao(10,5));

let multiplicacao = (a, b) => (a*b).toFixed(2);
console.log(multiplicacao(2,3.5));