let quantidade = 10;
let contador = 10;

// o loop while não executaria nenhuma vez, pois a condição é verificada antes do primeiro loop que no caso  é falsa
do  {
    console.log(contador);
    contador++;
} while (contador < quantidade);